{
	"name": "plutus-sls",
	"version": "0.1.0",
	"description": "",
	"main": "index.js",
	"scripts": {
		"deploy-saveLatestRates": "gcloud functions deploy saveLatestRates --runtime nodejs10 --trigger-http --env-vars-file .env.yaml --region europe-west2",
		"deploy-getLatestRates": "gcloud functions deploy getLatestRates --runtime nodejs10 --trigger-http --env-vars-file .env.yaml --region europe-west2"
	},
	"author": "serverless.com",
	"license": "MIT",
	"devDependencies": {
		"serverless-google-cloudfunctions": "*"
	},
	"dependencies": {
		"axios": "^0.19.0",
		"pg": "^7.11.0",
		"pg-hstore": "^2.3.3",
		"sequelize": "^5.8.9",
		"uuid": "^3.3.2"
	}
}
